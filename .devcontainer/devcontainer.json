{
  "name": "markdown2pdf",
  "image": "mcr.microsoft.com/devcontainers/go:1.25",
  "features": {
    "ghcr.io/devcontainers/features/node:1": {}
  },
  "postCreateCommand": "sudo rm -f /etc/apt/sources.list.d/yarn.list && sudo apt-get update && sudo apt-get install -y --no-install-recommends chromium && go install github.com/golangci/golangci-lint/v2/cmd/golangci-lint@v2.10.1 && go install golang.org/x/vuln/cmd/govulncheck@latest && go install github.com/fzipp/gocyclo/cmd/gocyclo@latest && go install github.com/gordonklaus/ineffassign@latest && go install github.com/client9/misspell/cmd/misspell@latest && npm install -g @mermaid-js/mermaid-cli && go mod download",
  "remoteEnv": {
    "GOTOOLCHAIN": "auto",
    "CHROMIUM_FLAGS": "--no-sandbox --disable-dev-shm-usage",
    "PUPPETEER_EXECUTABLE_PATH": "/usr/bin/chromium"
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "golang.go",
        "bierner.markdown-mermaid",
        "github.vscode-github-actions"
      ]
    }
  }
}
